[tool.poetry]
name = "dbt-server"
version = "0.1.0"
description = "An extension for the dbt CLI to run jobs remotely"
authors = ["Alexis VIALARET <alexis.vialaret@artefact.com>"]
license = "Apache2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
dbt-bigquery = "^1.8.1"
python-multipart = "^0.0.9"
humanize = "^4.9.0"
google-cloud-iam = "^2.15.0"
google-cloud-compute = "^1.19.0"
google-cloud-run = "^0.10.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
